import React from "react";

const Selector = () => {
  return (
    <button
      style={{
        height: "50px",
        width: "90px",
        padding: "5px",
        borderRadius: "5px",
        backgroundColor: "blue",
        color: "white",
        cursor: "pointer",
        border: "none",
        fontSize: "small",
      }}
    >
      Lucky Person
    </button>
  );
};

export default Selector;
